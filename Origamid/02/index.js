class Minhaclas{
  constructor(links){ //é a primeira coisa aser executa e onde se coloca as principai configuraçoes da clase
  this.links = outracoisa;
  }
}
const scroll = new Minhaclas('a');

console.log(scroll.links);